name: cra-dev
channels:
  - conda-forge
dependencies:
  - python < 3.14
  - pip
  - compas >=2.4
  # ipopt for editable installs, which carry no bundled binary; wheels ship their own
  - ipopt
  - pip:
      - compas_assembly >=0.7.0
      - matplotlib
      - -e .[dev]
